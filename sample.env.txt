#Youtube Data API v3 API key
YOUTUBE_API_KEY= (insert your youtube data api key here)

#Postgre Database syntax for local DB postgresql://<username>:<password>@localhost:<port>/<dbname>
DATABASE_URL=postgresql://postgres:<username>@localhost:<port>/<dbname>

#Region code (only interested in PH)
REGION_CODE=PH
